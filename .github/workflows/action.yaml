name: Integration Testing
on:
  push:
    branches:    
      - gitaction_devops
      - github_actions_test
jobs:
  stage-one:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Build Packages
      run: |
        python3 --version
        ls
        cd torch_ort_inference
        python build.py
        cd dist
        ls
